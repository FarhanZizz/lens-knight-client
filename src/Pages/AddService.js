import React from 'react';

const AddService = () => {
    return (
        <div>
            <h1 className='text-4xl text-center font-bold'>add a service</h1>
        </div>
    );
};

export default AddService;